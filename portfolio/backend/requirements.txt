# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
slowapi>=0.1.9

# --- Config / validation ---
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# --- OpenAI (chat + embeddings) ---
openai>=1.55.0
tenacity>=9.0.0

# --- Vector DB (dense + integrated sparse + hosted rerank) ---
pinecone>=6.0.0

# --- HTTP ---
httpx>=0.27.0

# --- Dev / test ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
